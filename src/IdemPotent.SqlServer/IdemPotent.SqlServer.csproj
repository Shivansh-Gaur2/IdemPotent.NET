<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFramework>net8.0</TargetFramework>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<PackageId>IdemPotent.SqlServer</PackageId>
		<Description>SQL Server-backed idempotency store with coordinated expiry cleanup for ASP.NET Core APIs.</Description>
		<PackageTags>idempotency;aspnetcore;sqlserver;database;api</PackageTags>
	</PropertyGroup>

	<ItemGroup>
		<ProjectReference Include="..\IdemPotent.Core\IdemPotent.Core.csproj" />
	</ItemGroup>

	<ItemGroup>
		<FrameworkReference Include="Microsoft.AspNetCore.App" />
	</ItemGroup>

	<ItemGroup>
		<PackageReference Include="Microsoft.Data.SqlClient" Version="5.2.2" />
	</ItemGroup>

	<ItemGroup>
	  <EmbeddedResource Include="Scripts\CreateIdempotencyTable.sql">
	    <LogicalName>IdemPotent.SqlServer.Scripts.CreateIdempotencyTable.sql</LogicalName>
	  </EmbeddedResource>
	</ItemGroup>

</Project>
